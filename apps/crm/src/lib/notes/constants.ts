// Note images are stored as `document` rows (reusing the authenticated blob
// proxy) but tagged with this title so they can be filtered out of the tenant's
// 문서 tab, where they'd otherwise appear as loose attachments.
export const NOTE_IMAGE_TITLE = "[note-image]";
